import Gallery from './Gallery'

export default function GalleryPage() {
  return (
    <main>
      {/* The Gallery component now includes its own dedicated Hero section as per the UI design */}
      <Gallery />
    </main>
  )
}
